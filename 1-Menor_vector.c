#include<stdio.h>
#include<stdlib.h>
//Hernandez Hernandez Erik Jose
int posicion,i,j;

main()
{
    int m[10];
    int menor = m[0];
    for (i=0; i<=10; i++)
    {
        printf("ingresa un numero: ");
        scanf("%d",&m[i]);
    }
    for (j = 1; j < sizeof(m) / sizeof(m[0]; j++)
{
    if (m[j] > mayor)
        {
            mayor = m[j];
        }
        //if (m[i] < menor) menor = m[i];
    }
    return 0;
}

